namespace MmcSerializer.Adapters
{
    /// <summary>
    /// Adapter that allows for serilization to and from a json file.
    /// </summary>
    public class JsonSerializerAdapter : ISerializerAdapter
    {
        public void Serialize(SerializationNode rootNode)
        {
            throw new NotImplementedException();
        }

        public SerializationNode? Deserialize()
        {
            throw new NotImplementedException();
        }
    }
}
